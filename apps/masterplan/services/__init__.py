"""MasterPlan domain services.

Previously a byte-identical copy of ``projection_service`` — see the note in
``apps/masterplan/__init__.py``. Import from the owning module:
``from apps.masterplan.services.projection_service import evaluate_phase``.
"""
